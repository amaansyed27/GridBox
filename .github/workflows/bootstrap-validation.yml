name: Bootstrap validation report

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporarily paused while source fixes are applied."
